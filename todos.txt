todos:
    tests
    header.html
    <!-- <meta property='og:image' content='{{ config.logo }}'> todo: add  The recommended resolution for the OG image is 1200x627 pixels, the size — up to 5MB.  -->
    <!-- todo: twitter cards https://developer.twitter.com/en/docs/tweets/optimize-with-cards/guides/getting-started -->
    <!--
    <link rel="manifest" href="{{ config.base_url }}static/config.webmanifest">
    <link rel="mask-icon" href="{{ config.base_url }}static/safari-pinned-tab.svg" color="#5bbad5">
    <link rel="apple-touch-icon" sizes="180x180" href="{{ config.base_url }}static/styles/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="{{ config.base_url }}static/styles/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="{{ config.base_url }}static/favicon-16x16.png">
    -->

    urls
    tags
    tests
    statics
    navigation
    better config
    url validation
    sitemap generator
    disable i18n option
    write docs about i18n
    add hot auto reload during dev
    add docs about helper functions
    documentation: creating templates
    inform that date is a required field
    remember locale selection with local storage
    extend custom functions to allow other helpers
    check if page slug conflicts with article slugs/urls
    add extended i18n to md files (prerender the md with jinja)
    rendering different sections with different mds in one page

    locale auto redirection for user browser language  # DONE
    if not github/ln links: dont display  # DONE
    just_titles: true/false  # DONE
    live server  # DONE
    i18n  # DONE
